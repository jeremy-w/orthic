_site
.sass-cache
.jekyll-cache
.jekyll-metadata
vendor

# generated by mkcert localhost
# jekyll's --ssl-host and --ssl-key args seem to ignore any path to the file,
# so the the files have to be at the top level, and you can't just conveniently
# stick them outside the repo.
*.pem
